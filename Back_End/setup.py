from setuptools import setup, find_packages

setup(
    name="timeweaver_backend",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[
        # Dependencies are in requirements.txt
    ],
)
